import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "sims4-random-build-generator",
  outDir: './dist/static',
  routes: {
  }
};